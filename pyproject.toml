[project]
name = "flappy"
version = "0.1.0"
description = "litel flapy lol"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pygame>=2.6.1",
    "numpy"
]

[tool.uv]
package = false
